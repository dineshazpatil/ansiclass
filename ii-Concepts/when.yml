---
-
  hosts: all
  gather_facts: false # this will make not to gather facts before running tasks

  tasks: 
    - name: Display Register value
      win_shell: get-service
      register: hostnamevar
    
    - name: Show output
      debug:
        var: hostnamevar
      when: hostnamevar.state == running     
